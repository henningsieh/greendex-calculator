# Original request

Now that I have a VERY roick-solid codebase foundation - clean setup, good tests, very modularized, no redundancies, plausible agents setup and context, etc... - with all that being set up, I now want to start  
to _import_ the "real" use-cases from a vibe-coded clickdummy.

This clickdummy, vibecoded by a friend, is the "truth" in terms of use-cases! The clickdummy is bad in terms of code quality, ui flows and ui structure, etc... but the clickdummy is the master of domain  
knowledge and requirements for the stuff, that should and can be done with the app.

Now I want know... the questions are: how to

- prepare the codebase and how to reference the clickdummý in the @apps/cost-tracker/docs folder
- compare the current MVP state of this `cost-tracker` app with the use-cases in the clickdummy
- start including the real use-cases from clickdummy into this cost-tracker app

The thing is, the clickdummy has already made bad decisions. for example the fact, that all use-cases for ui users and organozations are ALL made via "click on a card". Not very intuitive UI! The vibe-coder has knowledge about the domain requirements and use-cases, but - in my eyes - but is not an experienced developer.
